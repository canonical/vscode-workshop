name: ext
base: ubuntu@24.04
sdks:
  - name: node
    channel: "24"
  - name: project-test-deps
actions:
  build: |
    npm ci
    npx @vscode/vsce package
  test: |
    npm run test
  test-ci: |
    xvfb-run -a npm test
